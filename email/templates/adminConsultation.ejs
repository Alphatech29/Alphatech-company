<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>New Consultation Booking | <%= site_name %></title>

  <style>
    body {
      font-family: 'Inter', Arial, sans-serif;
      background: #f4f4f8;
      margin: 0;
      padding: 40px 20px;
      color: #222;
    }

    .container {
      max-width: 640px;
      margin: auto;
      background: #fff;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 10px 35px rgba(0, 0, 0, 0.06);
      border: 1px solid rgba(0, 0, 0, 0.05);
    }

    .header {
      background: linear-gradient(135deg, #4B0082, #DAA520);
      color: #fff;
      text-align: center;
      padding: 40px 20px 30px;
    }

    .header img {
      width: 8rem;
      height: auto;
      margin-bottom: 15px;
    }

    .header h1 {
      font-size: 24px;
      margin: 5px 0;
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    .header p {
      font-size: 15px;
      margin: 0;
      opacity: 0.9;
    }

    .content {
      padding: 35px 30px;
    }

    .message {
      font-size: 15px;
      color: #444;
      line-height: 1.7;
    }

    .details-card {
      background: #faf8ff;
      border-left: 5px solid #4B0082;
      border-radius: 10px;
      padding: 20px;
      margin: 25px 0;
    }

    .details-card h3 {
      margin-top: 0;
      font-size: 16px;
      color: #4B0082;
    }

    .details-card p {
      margin: 6px 0;
      font-size: 14px;
      color: #333;
    }

    .footer {
      background: #f7f7f9;
      text-align: center;
      padding: 30px 20px;
      font-size: 13px;
      color: #555;
      line-height: 1.7;
      border-top: 1px solid #e0e0e0;
    }

    .footer a {
      color: #DAA520;
      text-decoration: none;
      margin: 0 5px;
      font-weight: 500;
    }

    .footer a:hover {
      text-decoration: underline;
    }

    @media (max-width: 600px) {
      .content {
        padding: 25px;
      }

      .header h1 {
        font-size: 22px;
      }
    }
  </style>
</head>

<body>
  <div class="container">
    <!-- HEADER -->
    <div class="header">
      <img src="<%= site_url %><%= avatar %>" alt="<%= site_name %> Logo" />
      <h1>New Consultation Booking</h1>
      <p>A user has successfully booked a consultation.</p>
    </div>

    <!-- CONTENT -->
    <div class="content">
      <div class="message">
        <p>
          Hello Admin,  
          <br><br>
          A new consultation has just been booked on <strong><%= site_name %></strong>. Below are the details of the booking:
        </p>

        <div class="details-card">
          <h3>👤 Client Information</h3>
          <p><strong>Full Name:</strong> <%= full_name %></p>
          <p><strong>Email:</strong> <%= email %></p>
        </div>

        <div class="details-card">
          <h3>📅 Booking Details</h3>
          <p><strong>Date:</strong> <%= date %></p>
          <p><strong>Time:</strong> <%= time %></p>
          <p><strong>Duration:</strong> <%= duration %></p>
          <p><strong>Mode:</strong> <%= mode %></p>
          <p><strong>Amount Paid:</strong> <%= cost %></p>
          <p><strong>Transaction ID:</strong> <%= transaction_id %></p>
        </div>

        <p>
          Please ensure to confirm the session with the client and prepare any necessary materials or communication links prior to the scheduled time.
        </p>

        <p style="margin-top: 10px;">
          You can reach the client via the email provided above if additional clarification is required.
        </p>
      </div>
    </div>

    <!-- FOOTER -->
    <div class="footer">
      <p>
        © <%= new Date().getFullYear() %>
        <strong><%= site_name %></strong>. All Rights Reserved.<br />
        <em>This is an automated admin notification. Please do not reply.</em>
      </p>

      <p style="margin-top: 8px;">
        <a href="<%= site_url %>">Visit Website</a> |
        <a href="<%= site_url %>/admin">Admin Dashboard</a>
      </p>
    </div>
  </div>
</body>

</html>
