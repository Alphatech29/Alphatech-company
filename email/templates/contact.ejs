<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>New Contact Submission | AlphaTech Multimedia</title>

    <style>
      body {
        font-family: 'Inter', Arial, sans-serif;
        background: linear-gradient(135deg, #f3f4f6 0%, #e9ecf2 100%);
        margin: 0;
        padding: 40px 20px;
        color: #333;
      }

      .wrapper {
        max-width: 640px;
        margin: auto;
        background: #ffffffcc;
        backdrop-filter: blur(10px);
        border-radius: 20px;
        overflow: hidden;
        box-shadow: 0 12px 30px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(0, 0, 0, 0.05);
      }

      .header {
        background: linear-gradient(135deg, #4B0082, #DAA520);
        color: #fff;
        text-align: center;
        padding: 40px 20px;
      }

      .header h1 {
        font-size: 24px;
        margin: 0;
        font-weight: 700;
      }

      .header p {
        font-size: 15px;
        margin-top: 8px;
        opacity: 0.95;
      }

      .content {
        padding: 30px;
      }

      .section {
        margin-bottom: 30px;
      }

      .section-title {
        font-size: 18px;
        font-weight: 700;
        color: #222;
        margin-bottom: 16px;
        position: relative;
        display: inline-block;
      }

      .section-title::after {
        content: "";
        position: absolute;
        left: 0;
        bottom: -4px;
        width: 40%;
        height: 3px;
        background: linear-gradient(90deg, #4B0082, #DAA520);
        border-radius: 4px;
      }

      .info-card {
        background: #fafaff;
        border: 1px solid #ebe7ff;
        border-radius: 12px;
        padding: 20px;
      }

      .info-row {
        margin-bottom: 12px;
      }

      .info-row strong {
        display: block;
        color: #4B0082;
        font-weight: 600;
        margin-bottom: 4px;
      }

      .info-row span {
        color: #444;
      }

      .message-block {
        background: #fffdf2;
        border-left: 5px solid #DAA520;
        border-radius: 10px;
        padding: 18px 20px;
        font-size: 15px;
        line-height: 1.6;
        color: #333;
        white-space: pre-wrap;
      }

      .footer {
        background: #f9f9fb;
        text-align: center;
        padding: 24px;
        border-top: 1px solid #eee;
        font-size: 13px;
        color: #666;
      }

      .footer strong {
        color: #4B0082;
      }

      .footer a {
        color: #DAA520;
        text-decoration: none;
        margin: 0 6px;
        font-weight: 500;
      }

      .footer a:hover {
        text-decoration: underline;
      }

      @media (max-width: 600px) {
        .content {
          padding: 20px;
        }

        .header h1 {
          font-size: 20px;
        }

        .section-title {
          font-size: 16px;
        }
      }
    </style>
  </head>

  <body>
    <div class="wrapper">
      <div class="header">
        <h1>New Contact Submission</h1>
        <p>A new inquiry just arrived from your website contact form</p>
      </div>

      <div class="content">
        <div class="section">
          <h2 class="section-title">Sender Details</h2>
          <div class="info-card">
            <div class="info-row">
              <strong>Name</strong> <span><%= name %></span>
            </div>
            <div class="info-row">
              <strong>Email</strong> <span><%= email %></span>
            </div>
            <div class="info-row">
              <strong>Subject</strong> <span><%= subject %></span>
            </div>
          </div>
        </div>

        <div class="section">
          <h2 class="section-title">Message</h2>
          <div class="message-block"> <%- message
          .split(/\n\s*\n/)
          .map(p => `<p style="margin:0 0 10px 0;">${p.trim().replace(/\n/g, '<br>')}</p>`)
          .join('') %></div>
        </div>
      </div>

      <div class="footer">
        <p>
          © <%= new Date().getFullYear() %> <strong><%= site_name %></strong><br />
          This notification was automatically generated by your contact form.
        </p>
        <p>
          <a href="<%= site_url %>">Visit Website</a> |
          <a href="mailto:<%= contact_email %>">Contact Support</a>
        </p>
      </div>
    </div>
  </body>
</html>
